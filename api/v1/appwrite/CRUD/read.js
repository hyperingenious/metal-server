const sdk = require("node-appwrite");

/*
// Example: Read a document in a collection
async function readExampleDocument(documentId) {
    try {
        const document = await databases.getDocument(
            DATABASE_ID,
            EXAMPLE_COLLECTION_ID, // Use your collection ID here
            documentId
        );
        return document;
    } catch (err) {
        console.error("Failed to read example document:", err);
        throw err;
    }
}
*/
