const sdk = require("node-appwrite");

/*
// Example: Delete a document in a collection
async function deleteExampleDocument(documentId) {
    try {
        const deleted = await databases.deleteDocument(
            DATABASE_ID,
            EXAMPLE_COLLECTION_ID, // Use your collection ID here
            documentId
        );
        return deleted;
    } catch (err) {
        console.error("Failed to delete example document:", err);
        throw err;
    }
}
*/
