const sdk = require("node-appwrite");

/*
// Example: Update a document in a collection
async function updateExampleDocument(documentId, data) {
    try {
        const document = await databases.updateDocument(
            DATABASE_ID,
            EXAMPLE_COLLECTION_ID, // Use your collection ID here
            documentId,
            data
        );
        return document;
    } catch (err) {
        console.error("Failed to update example document:", err);
        throw err;
    }
}
*/
