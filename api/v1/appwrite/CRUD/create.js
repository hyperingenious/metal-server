const sdk = require("node-appwrite");

/*
// Example: Create a document in a collection
async function createExampleDocument(data) {
    try {
        const document = await databases.createDocument(
            DATABASE_ID,
            EXAMPLE_COLLECTION_ID, // Use your collection ID here
            sdk.ID.unique(),
            data
        );
        return document;
    } catch (err) {
        console.error("Failed to create example document:", err);
        throw err;
    }
}
*/